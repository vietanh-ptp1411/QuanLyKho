<UserControl x:Class="QuanLyKho.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:System="clr-namespace:System;assembly=mscorlib">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Tổng quan" Style="{StaticResource PageTitle}"/>

            <!-- Row 1: 3 cards -->
            <UniformGrid Columns="3" Margin="0,0,0,12">
                <materialDesign:Card Padding="18" Margin="0,0,10,0">
                    <StackPanel>
                        <DockPanel>
                            <Border Width="42" Height="42" CornerRadius="10" Background="#E3F2FD" DockPanel.Dock="Left">
                                <materialDesign:PackIcon Kind="CubeOutline" Width="24" Height="24" Foreground="#1565C0"
                                                         HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Tổng số vật tư" FontSize="12" Foreground="Gray"/>
                                <TextBlock Text="{Binding TongSoVatTu}" FontSize="28" FontWeight="Bold"/>
                            </StackPanel>
                        </DockPanel>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Padding="18" Margin="0,0,10,0">
                    <DockPanel>
                        <Border Width="42" Height="42" CornerRadius="10" Background="#FFF3E0" DockPanel.Dock="Left">
                            <materialDesign:PackIcon Kind="Warehouse" Width="24" Height="24" Foreground="#E65100"
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Số kho" FontSize="12" Foreground="Gray"/>
                            <TextBlock Text="{Binding TongSoKho}" FontSize="28" FontWeight="Bold"/>
                        </StackPanel>
                    </DockPanel>
                </materialDesign:Card>

                <materialDesign:Card Padding="18">
                    <DockPanel>
                        <Border Width="42" Height="42" CornerRadius="10" Background="#E8F5E9" DockPanel.Dock="Left">
                            <materialDesign:PackIcon Kind="CalendarMonth" Width="24" Height="24" Foreground="#2E7D32"
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Tháng hiện tại" FontSize="12" Foreground="Gray"/>
                            <TextBlock Text="{Binding Source={x:Static System:DateTime.Now}, StringFormat='{}{0:MM/yyyy}'}"
                                       FontSize="28" FontWeight="Bold"/>
                        </StackPanel>
                    </DockPanel>
                </materialDesign:Card>
            </UniformGrid>

            <!-- Row 2: Nhập / Xuất tháng -->
            <UniformGrid Columns="2">
                <materialDesign:Card Padding="18" Margin="0,0,10,0">
                    <DockPanel>
                        <Border Width="42" Height="42" CornerRadius="10" Background="#E8F5E9" DockPanel.Dock="Left">
                            <materialDesign:PackIcon Kind="PackageDown" Width="24" Height="24" Foreground="#2E7D32"
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Nhập kho tháng này" FontSize="12" Foreground="Gray"/>
                            <TextBlock FontSize="18" FontWeight="Bold" Margin="0,2,0,0">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} phiếu  -  {1:N0} đ">
                                        <Binding Path="SoPhieuNhapThang"/>
                                        <Binding Path="TongGiaTriNhapThang"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </DockPanel>
                </materialDesign:Card>

                <materialDesign:Card Padding="18">
                    <DockPanel>
                        <Border Width="42" Height="42" CornerRadius="10" Background="#FFEBEE" DockPanel.Dock="Left">
                            <materialDesign:PackIcon Kind="PackageUp" Width="24" Height="24" Foreground="#C62828"
                                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Xuất kho tháng này" FontSize="12" Foreground="Gray"/>
                            <TextBlock FontSize="18" FontWeight="Bold" Margin="0,2,0,0">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0} phiếu  -  {1:N0} đ">
                                        <Binding Path="SoPhieuXuatThang"/>
                                        <Binding Path="TongGiaTriXuatThang"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </DockPanel>
                </materialDesign:Card>
            </UniformGrid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
